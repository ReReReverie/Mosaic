# View analysis

**Status: In the works.**

The per-reference "View analysis" drawer is temporarily removed from the UI
while its interaction and rendering behavior are being refined. The underlying
reference analysis, scores, reasons, prompt evaluation, and synthesis remain
available through the board and exports.

This feature will return after the interaction is stable across the home board,
the saved board, and legacy analysis results.
