# Optional: enables GPT-assisted brief interpretation. Leave blank for the
# local demo; the deterministic interpreter remains available without it.
OPENAI_API_KEY=

# Optional: Neon PostgreSQL connection string. Leave blank for now. Keep it
# server-side; never prefix it with NEXT_PUBLIC_.
DATABASE_URL=
