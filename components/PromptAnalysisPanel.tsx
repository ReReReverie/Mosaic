import type { PromptAnalysis } from "@/core/types";

// Prompt profile details are intentionally hidden from the current board UI.
export function PromptAnalysisPanel(_props: { analysis: PromptAnalysis }) {
  void _props;
  return null;
}
