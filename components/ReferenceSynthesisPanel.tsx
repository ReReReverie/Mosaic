import type { ReferenceSynthesis } from "@/core/types";

// Set-level synthesis details are intentionally hidden from the current board UI.
export function ReferenceSynthesisPanel(_props: { synthesis: ReferenceSynthesis }) {
  void _props;
  return null;
}
